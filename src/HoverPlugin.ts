import { HoverPluginImpl } from "./HoverPluginImpl";

/**
 * ホバー機能を提供するプラグイン。
 * 実装は HoverPluginImpl だが互換性維持のため残しています。
 */
export class HoverPlugin extends HoverPluginImpl {}

module.exports = HoverPlugin;
